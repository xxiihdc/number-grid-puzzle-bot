interface:
  display_name: "Analyze Latest Training Run"
  short_description: "Diagnose a Number Grid Puzzle training run"
  default_prompt: "Use $matrix-analyze-latest-training-run to analyze the latest persisted GA run and produce a reusable agent-handoff report without promoting weights."
